# 컬러 팔레트 디자인 시스템

## 프로젝트 정보
- **출처 웹사이트**: AI SEARCH OPTIMIZATION LAB by MASSTIGE (image.png 분석)
- **분석 날짜**: 2026-08-26
- **적용 프로젝트**: 포트폴리오 웹사이트

## CSS 변수 정의
```css
:root {
  /* Primary Colors */
  --color-primary: #DDFF50;        /* 라임 옐로우그린 - 메인 브랜드 컬러 */
  --color-primary-light: #E9FF8D;  /* 라임 밝은 톤 - hover/배경용 */
  --color-primary-dark: #9BB238;   /* 라임 어두운 톤 - active/텍스트 대비용 */

  /* Secondary Colors */
  --color-secondary: #0B0B0B;      /* 딥 블랙 - 서브 배경/텍스트 */
  --color-accent: #CAB8F6;         /* 라벤더 퍼플 - 포인트 강조 */

  /* Background Colors */
  --color-bg-primary: #0B0B0B;     /* 메인 배경 (다크) */
  --color-bg-secondary: #191919;   /* 카드/섹션 배경 (다크 그레이) */

  /* Text Colors */
  --color-text-primary: #FFFFFF;   /* 다크 배경 위 기본 텍스트 */
  --color-text-secondary: #0B0B0B; /* 밝은 컬러 배경 위 기본 텍스트 */
  --color-text-muted: #919191;     /* 보조 설명/캡션 텍스트 */

  /* Interactive Colors */
  --color-button-primary: #DDFF50; /* 기본 버튼 배경 */
  --color-button-hover: #BCD944;   /* 버튼 hover 상태 */
  --color-link: #CAB8F6;           /* 링크 기본 색상 */
  --color-link-hover: #A293C5;     /* 링크 hover 상태 */
}
```

## 컬러 사용 가이드

### Primary (`--color-primary` #DDFF50)
- 히어로 섹션 배경, 메인 CTA 버튼, 강조 숫자/카피 등 **가장 먼저 시선을 끌어야 하는 요소**에 사용합니다.
- 사용 빈도가 높을수록 브랜드 인지가 강해지지만, 남용하면 눈이 피로하므로 페이지당 1~2개 핵심 섹션으로 제한합니다.
- `--color-primary-light`는 라임 톤의 넓은 배경 영역(카드 배경, hover 시 배경 tint)에, `--color-primary-dark`는 라임 배경 위에 얹는 텍스트나 아이콘 테두리처럼 대비가 필요한 곳에 사용합니다.

### Secondary (`--color-secondary` #0B0B0B)
- 전체 레이아웃의 기본 배경색이자 밝은 컬러 카드 위의 텍스트 색으로 사용합니다.
- 블랙 배경 + 화이트 텍스트 조합은 콘텐츠의 "신뢰감/전문성"을 전달하는 섹션(회사 소개, 실적 지표 등)에 적합합니다.

### Accent (`--color-accent` #CAB8F6)
- STEP 배지, 링크, 보조 카드 배경 등 **정보 흐름의 단계나 이동 경로를 표시**할 때 사용합니다.
- Primary(라임)와 동시에 큰 면적으로 함께 쓰지 않고, 라임을 메인으로 뒀다면 퍼플은 서브 섹션 1곳 정도로 제한해 위계를 유지합니다.

### Background (`--color-bg-primary`, `--color-bg-secondary`)
- `--color-bg-primary`(#0B0B0B)는 페이지 전체 배경, `--color-bg-secondary`(#191919)는 그 위에 얹는 카드/모듈 배경으로 써서 미세한 깊이감(레이어 구분)을 만듭니다.
- 밝은 컬러 카드(라임/퍼플/시안)는 배경색이라기보다 "섹션 강조 블록"이므로 전체 배경으로 확대 사용하지 않습니다.

### Text (`--color-text-primary`, `--color-text-secondary`, `--color-text-muted`)
- 다크 배경 위에는 `--color-text-primary`(화이트), 밝은 컬러 카드 위에는 `--color-text-secondary`(블랙)를 사용해 항상 WCAG AA 이상의 명도 대비를 확보합니다.
- `--color-text-muted`는 날짜, 캡션, 부가 설명 등 정보 위계상 가장 낮은 텍스트에만 제한적으로 사용합니다.

### Interactive (`--color-button-primary`, `--color-button-hover`, `--color-link`, `--color-link-hover`)
- 기본 버튼은 라임(`--color-button-primary`) 배경에 블랙 텍스트 조합을 기본값으로 하고, hover 시 `--color-button-hover`로 톤을 살짝 낮춰 클릭 가능함을 시각적으로 피드백합니다.
- 텍스트 링크는 `--color-link`(라벤더)를 사용하고, hover 시 `--color-link-hover`로 어둡게 전환해 밟는 느낌을 줍니다.
- 버튼/링크 색은 절대 배경색과 동일하게 두지 않아 상호작용 요소가 항상 구분되도록 합니다.

## 반응형 고려사항

### 다크모드
- 원본 디자인 자체가 블랙 베이스이므로 **다크모드가 기본값**이며 별도 다크모드 전환 없이도 자연스럽게 적용됩니다.
- 라이트모드를 별도로 지원해야 한다면 `--color-bg-primary`/`--color-bg-secondary`만 화이트 계열(`#FFFFFF`, `#F5F5F5`)로 교체하고, `--color-text-primary`/`--color-text-secondary`를 서로 반전시키는 방식을 권장합니다. Primary(라임)와 Accent(퍼플)는 라이트모드에서도 채도를 유지해 브랜드 정체성을 지킵니다.
- CSS `prefers-color-scheme` 미디어 쿼리 또는 `data-theme` 속성으로 변수 값만 교체하면 마크업 수정 없이 테마 전환이 가능합니다.

```css
@media (prefers-color-scheme: light) {
  :root {
    --color-bg-primary: #FFFFFF;
    --color-bg-secondary: #F5F5F5;
    --color-text-primary: #0B0B0B;
    --color-text-secondary: #0B0B0B;
  }
}
```

### 화면 크기별 고려사항
- 모바일 등 좁은 화면에서는 라임/퍼플/시안 컬러 블록의 면적이 좁아지므로, 색상 대비만으로 정보 구분이 어려워지지 않도록 **테두리(`border`)나 여백**을 함께 활용합니다.
- 다크 배경 위 저채도 텍스트(`--color-text-muted`)는 작은 화면·저조도 환경에서 가독성이 떨어질 수 있으므로 폰트 크기 14px 이하로는 사용하지 않는 것을 권장합니다.
- 접근성을 위해 모든 버튼/링크 색상 조합은 최소 4.5:1 명도 대비를 유지하도록 실제 배포 전 대비 체커로 검증합니다.
