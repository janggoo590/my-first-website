# 컬러 팔레트 디자인 시스템

## 프로젝트 정보
- **출처 웹사이트**: AI SEARCH OPTIMIZATION LAB by MASSTIGE (AI최적화랩)
- **분석 날짜**: 2026-08-27
- **적용 프로젝트**: 포트폴리오 웹사이트
- **분석 방법**: `image.png` 스크린샷을 약 86,000개 지점 픽셀 샘플링하여 색상별 사용 비율 추출

## CSS 변수 정의
```css
:root {
  /* Primary Colors */
  --color-primary: #DDFF50;        /* 라임 옐로우그린 - 메인 브랜드 컬러, CTA */
  --color-primary-light: #E9FF8D;  /* 라임 밝은 톤 - hover 배경 tint, 넓은 배경 영역 */
  --color-primary-dark: #9BB238;   /* 라임 어두운 톤 - 라임 배경 위 아이콘/테두리 대비용 */

  /* Secondary Colors */
  --color-secondary: #0B0B0B;      /* 니어 블랙 - 다크 섹션 블록, 헤드라인 텍스트 */
  --color-accent: #CAB8F6;         /* 라벤더 퍼플 - 프로세스 단계(STEP) 강조, 포인트 */

  /* Background Colors */
  --color-bg-primary: #FFFFFF;     /* 메인 페이지 / 카드 기본 배경 (사용 비중 최상위) */
  --color-bg-secondary: #F7F7F5;   /* 대체 섹션 배경 - 살짝 웜한 오프화이트 */
  --color-bg-dark: #0B0B0B;        /* 다크 강조 섹션 배경 (신뢰/실적 블록) */
  --color-bg-dark-elevated: #191919; /* 다크 섹션 안 중첩 카드 배경 */

  /* Text Colors */
  --color-text-primary: #0B0B0B;   /* 밝은 배경 / 라임 배경 위 기본 텍스트 */
  --color-text-secondary: #FFFFFF; /* 다크 배경 위 기본 텍스트 */
  --color-text-muted: #8A8A8A;     /* 캡션, 보조 설명 등 위계가 낮은 텍스트 */

  /* Border Colors */
  --color-border: #ECEDEA;         /* 밝은 카드의 얇은 헤어라인 테두리 */
  --color-border-strong: #E5E5E5;  /* 구분선(divider), 조금 더 진한 경계 */
  --color-border-on-dark: rgba(255, 255, 255, 0.1); /* 다크 카드 위 경계선 */

  /* Interactive Colors */
  --color-button-primary: #DDFF50; /* 기본 버튼 배경 (텍스트는 #0B0B0B) */
  --color-button-hover: #C7E63F;   /* 버튼 hover - 라임 톤을 살짝 낮춤 */
  --color-button-dark: #0B0B0B;    /* 다크 pill 버튼 / 배지 배경 (텍스트는 #FFFFFF) */
  --color-link: #A293C5;           /* 텍스트 링크 - 라벤더 계열 (밝은 배경 대비 확보용 톤다운) */
  --color-link-hover: #7A6BA8;     /* 링크 hover - 더 어둡게 전환 */
}

/* 보조 파스텔 액센트 - 일러스트/소형 카드에서만 소량 사용 */
:root {
  --color-accent-cyan: #7FE0F0;    /* 시안 - 소형 강조 카드 */
  --color-accent-blue: #A8C7FF;    /* 라이트 블루 - 서비스 아이콘 일러스트 */
  --color-accent-peach: #FFCBA9;   /* 피치 - 서비스 아이콘 일러스트 */
  --color-accent-mint: #7FE0C1;    /* 민트 - 소형 강조 요소 */
}
```

## 컬러 사용 가이드

### Primary — 라임 옐로우그린 (`--color-primary` #DDFF50)
- **용도**: 히어로 섹션 배경, 메인 CTA 버튼, 강조 숫자/카피, 프로그레스바 채움색, 활성 네비게이션 표시 등 **가장 먼저 시선을 끌어야 하는 요소**.
- **사용 원칙**: 큰 면적으로 쓸 때는 페이지당 1~2개 핵심 섹션으로 제한한다. 남용하면 형광 톤 특성상 눈이 쉽게 피로해진다.
- **조합**: 라임 배경 위 텍스트는 항상 `--color-text-primary`(#0B0B0B) 블랙을 사용한다. 흰색 텍스트는 대비가 부족하다.
- `--color-primary-light`(#E9FF8D)는 hover 시 배경 tint나 넓은 라임 배경 영역에, `--color-primary-dark`(#9BB238)는 라임 배경 위에 얹는 아이콘 테두리·구분선처럼 추가 대비가 필요한 곳에 사용한다.

### Secondary — 니어 블랙 (`--color-secondary` #0B0B0B)
- **용도**: 본문 헤드라인 텍스트 색, 그리고 "신뢰감/전문성"을 전달하는 섹션(실적 지표, 서비스 축 설명)의 배경 블록.
- **사용 원칙**: 페이지 전체 배경으로 쓰지 않는다. 밝은 베이스 위에 **섹션 단위 다크 블록**으로 삽입해 리듬과 위계를 만든다.
- 다크 블록 내부에서는 `--color-bg-dark-elevated`(#191919)로 중첩 카드를 구분해 미세한 깊이감을 준다.

### Accent — 라벤더 퍼플 (`--color-accent` #CAB8F6)
- **용도**: STEP 배지, 프로세스 단계 카드, 보조 강조 등 **정보 흐름의 단계나 이동 경로를 표시**할 때.
- **사용 원칙**: 라임(Primary)과 큰 면적으로 동시에 두지 않는다. 라임을 메인으로 뒀다면 퍼플은 서브 섹션 1곳 정도로 제한해 위계를 유지한다.
- 퍼플 카드 위 텍스트는 블랙(#0B0B0B)을 사용한다.

### 보조 파스텔 액센트 (시안 / 블루 / 피치 / 민트)
- 서비스 아이콘 일러스트, 소형 카드의 다양성 표현에만 **소량** 사용한다(각각 전체의 1% 미만).
- 정보 위계를 나타내는 색이 아니므로 버튼·링크 등 상호작용 요소에는 쓰지 않는다.

### Background (`--color-bg-primary`, `--color-bg-secondary`)
- `--color-bg-primary`(#FFFFFF)가 기본 배경이며 전체의 약 40%를 차지한다. `--color-bg-secondary`(#F7F7F5)는 인접 섹션과 배경을 미세하게 구분해야 할 때 교차 사용한다.
- 밝은 컬러 카드(라임/퍼플/파스텔)는 배경이 아니라 "섹션 강조 블록"이므로 전체 배경으로 확대하지 않는다.

### Text (`--color-text-primary`, `--color-text-secondary`, `--color-text-muted`)
- 밝은 배경·라임 배경 위 → `--color-text-primary`(#0B0B0B).
- 다크 블록 위 → `--color-text-secondary`(#FFFFFF).
- `--color-text-muted`(#8A8A8A)는 날짜·캡션·부가 설명 등 정보 위계상 가장 낮은 텍스트에만 제한적으로 쓰고, 폰트 크기 14px 이하로는 사용하지 않는다(가독성/대비 저하).

### Border (`--color-border`, `--color-border-strong`, `--color-border-on-dark`)
- 밝은 배경의 카드 경계는 `--color-border`(#ECEDEA) 1px 헤어라인이 기본이다. 표·목록의 행 구분처럼 더 명확한 선이 필요하면 `--color-border-strong`(#E5E5E5)를 쓴다.
- 다크 블록 내부 요소의 경계는 `--color-border-on-dark`(rgba(255,255,255,0.1))로 은은하게 처리한다.

### Interactive (`--color-button-*`, `--color-link*`)
- 기본 버튼 = 라임 배경(`--color-button-primary`) + 블랙 텍스트. hover 시 `--color-button-hover`(#C7E63F)로 톤을 낮춰 클릭 가능함을 피드백한다.
- 다크 pill 버튼/배지 = `--color-button-dark`(#0B0B0B) 배경 + 화이트 텍스트. 밝은 섹션에서 보조 액션에 사용한다.
- 텍스트 링크는 `--color-link`(#A293C5)를 사용하고 hover 시 `--color-link-hover`(#7A6BA8)로 어둡게 전환한다. 원본의 라벤더 순색(#CAB8F6)은 흰 배경에서 4.5:1 대비를 넘지 못하므로, **본문 링크에는 톤다운한 값**을 쓰고 순색은 배경·배지 등 텍스트가 아닌 요소에만 사용한다.
- 버튼/링크 색은 배경색과 동일하게 두지 않아 상호작용 요소가 항상 구분되도록 한다.

## 반응형 고려사항

### 다크모드
- 원본은 **화이트 베이스 + 다크 섹션 블록** 구조이므로 기본값은 라이트 모드다.
- 다크모드를 지원한다면 배경/텍스트 변수만 교체하고 Primary(라임)·Accent(퍼플)는 채도를 유지해 브랜드 정체성을 지킨다.
- CSS `prefers-color-scheme` 미디어 쿼리 또는 `data-theme` 속성으로 변수 값만 바꾸면 마크업 수정 없이 전환된다.

```css
@media (prefers-color-scheme: dark) {
  :root {
    --color-bg-primary: #0B0B0B;
    --color-bg-secondary: #191919;
    --color-text-primary: #FFFFFF;
    --color-text-secondary: #0B0B0B;
    --color-border: rgba(255, 255, 255, 0.1);
    --color-border-strong: rgba(255, 255, 255, 0.16);
    --color-link: #CAB8F6;        /* 다크 배경에서는 라벤더 순색이 충분한 대비를 가진다 */
    --color-link-hover: #E0D6FB;
  }
}
```

### 화면 크기별 고려사항
- 모바일 등 좁은 화면에서는 라임/퍼플/파스텔 컬러 블록의 면적이 줄어들어 색상 대비만으로 정보 구분이 어려워질 수 있으므로 **테두리(`border`)와 여백**을 함께 활용한다.
- 다크 블록은 모바일에서 상하 패딩을 넉넉히 유지해 밝은 섹션과의 경계가 뭉개지지 않도록 한다.
- `--color-text-muted`는 작은 화면·저조도 환경에서 가독성이 떨어지므로 14px 이하로 쓰지 않는다.
- 모든 버튼/링크 색상 조합은 최소 4.5:1 명도 대비를 유지하도록 배포 전 대비 체커로 검증한다.

## 컬러 사용 비율 (픽셀 샘플링 기준)

| 색상 그룹 | 대략 비율 | 주 사용 목적 |
|---|---|---|
| 화이트 계열 (`#FFFFFF` + `#F7F7F5`) | ~58% | 페이지·카드 바탕, 여백 |
| 라임 `#DDFF50` | ~13% | 히어로, CTA, 수치 강조 |
| 블랙 계열 (`#0B0B0B` + `#191919`) | ~11% | 다크 강조 섹션, 헤드라인 텍스트 |
| 라벤더 `#CAB8F6` | ~3% | STEP 단계 카드, 포인트 |
| 파스텔 액센트 (시안·블루·피치·민트) | ~3% | 아이콘 일러스트, 소형 카드 |
| 텍스트·테두리 그레이 | ~2% | 캡션, 구분선 |

## 디자인 특징

- **하이 콘트라스트 네온 + 미니멀 에디토리얼**: 넓은 화이트 여백 위에 형광 라임과 딥 블랙을 큰 면적으로 충돌시켜, 절제된 레이아웃에서도 강한 시선 집중과 에너지를 만든다. IT·AI 스타트업 특유의 자신감 있고 혁신적인 인상.
- **파스텔 액센트의 완충 역할**: 라벤더·시안·피치 같은 저채도 파스텔이 라임/블랙의 강한 대비를 누그러뜨려 친근하고 접근하기 쉬운 현대적 느낌을 더한다.
- **색이 곧 정보 구조**: 다크 블록 = 신뢰/실적, 라임 = 행동 유도(CTA), 퍼플 = 프로세스 단계. 색상만으로 콘텐츠의 역할을 구분할 수 있다.
- **테마 확장성**: 블랙·라임·퍼플이 이미 다크 팔레트와 호환되어, 배경만 반전하면 라이트/다크 양쪽에서 브랜드 정체성이 유지된다.
